import { StyleSheet } from "react-native"

export default styles = StyleSheet.create({
    container: {
        // backgroundColor: 'red',
        flex: 1,
        marginHorizontal: 20,
    },
    line: {
        borderWidth: 1,
        borderColor: 'lightgrey',
    },
    
});